<% provide(:title, 'Tienda') %>
<h1>Nueva Tienda</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@store) do |f| %>

      <!%= render 'shared/error_messages' %>
      <%= f.label "Nombre" %>
      <%= f.text_field :name %>

      <%= f.label "Dirección" %>
      <%= f.text_field :address %>

      <%= f.submit "Crear Tienda", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>